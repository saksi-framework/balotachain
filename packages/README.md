# Packages

Reusable Tala framework packages live here. Rust packages hold cryptographic
and credential primitives, Go packages hold bulletin board chaincode and
clients, and TypeScript packages hold shared protocol types.

