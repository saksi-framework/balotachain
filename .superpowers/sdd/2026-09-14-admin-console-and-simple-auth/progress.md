# Ledger — 2026-09-14 admin console + simple auth
Plan: docs/plans/2026-09-14-admin-console-and-simple-auth.md (approved 2026-09-14)
USER DECISIONS: approve plan; merge #54; D1 = read-only Population step (no real voter credentials in admin); D2 = open ci/audit-trail -> main docs PR now.
T0: PR #54 squash-merged -> balotachain main 34b1a92 (repo disallows merge commits).
T1: dispatched (executor opus, worktree saksi-auth, branch feat/console-auth off saksi main 076b730) — reviewer: final-reviewer opus, security focus.
T2: dispatched (executor opus, worktree balotachain-admin, branch feat/admin-console off balotachain main 34b1a92) — parallel to T1 against plan §4 contract; login optional when /api/me 404s — reviewer: sonnet.
Ruling: plan written to a file, not plan mode, because the 10K validation agent (resumed by user) was live. Costs if wrong: none.
Ruling: #54 squash-merged, not merge-commit, per repository settings. Costs if wrong: none (matches #53).
T5 (D2): docs PR #55 opened ci/audit-trail -> main (docs + run artifacts; includes approved plan a94be78). Merge awaits user go; repo is squash-only.
T1: implemented saksi PR #42 (feat/console-auth: 20c4155 + 10e5186). auth.go authorize()+routeAccess keyed by mux pattern, unknown route -> admin (fail closed); share ownership checked in handleCeremonySubmit; users-file validation; per-IP lockout 5 failures/5 min -> 429 30 s; auth off -> auth routes 404 (T2 degrade signal). CI 8/8, -race clean. Concerns raised: (a) /api/trail/<id>?operator=1 public per plan, unseals trail for anonymous loopback; (b) loopback shares one lockout counter.
Ruling: (a) with auth on, operator mode requires an admin session; (b) lockout keyed by IP+username with a looser per-IP cap. Both tighten, not loosen, plan §4; to be confirmed by the security review before the fix round. Costs if wrong: one extra fix round.
T1: security review dispatched (final-reviewer opus): bypass attempts, trustee ownership, sessions/cookies, login timing/lockout, auth-off parity, 7(a)/7(b).
T1 security review (opus): no authorization bypass (1,404 raw-socket probes: path/method/encoding tricks, static prefixes, absolute URIs); share ownership holds (same variable checked and used, exact map key); sessions/cookies/login timing sound; auth off is a true no-op. NEEDS FIXES: I1 operator=1 unseals trail for anonymous loopback with auth on (reproduced); I2 shared loopback lockout counter (reproduced). Minors: M1 publish not scoped to run's trustees (reproduced), M2 users-file gaps (dup trustee_id, digits-only, bcrypt cost bound), M3 embedded ServeMux bypasses route recording, M4 escaped-path comment, M5 SSE outlives logout (deferred), M6 cookie not port-isolated (runbook line).
T1: fix round 1 sent (I1, I2 with enumeration/order/size/reset conditions, M1, M2, M3, M4, M6; M5 deferred).
T2: implemented balotachain PR #56 (feat/admin-console: 7fdc7d9 admin, 8b81a97 trustee). Screens: sign-in, Elections, 1 Election, 2 Population (check API + election.csv; proof counts labelled derived; gate blocks Next), 3 Run, 4 Ceremony, 5 Results; trustee sign-in bound to session, read-only for admins. Auth-off degrade on /api/me 404. Tests admin 54, trustee 32, auditor 28; CI 13/13 (fabric skipped on PRs). Full offline lifecycle run through the real client against a real console on port 8097 + headless Chromium through all 5 steps (2 UI bugs found and fixed). Deviation: Run posts /ceremony/start not /submit (on-chain /submit skips the ceremony; offline no-op; console wizard does the same). Concerns: VITE_CONSOLE_URL dual use (proxy target + client base) inherited from #54; on-chain path not exercised; /admin/ served only after T1 merges.
T2: review dispatched (sonnet).
T2 review (sonnet): APPROVED, 0 Critical, 0 Important. Every JSON contract matched against feat/console-auth; /ceremony/start deviation verified correct (executor.go Submit no-op offline, submitOnChain bypasses ceremony; wizard.html does the same); Population derivation correct; no XSS/credential storage; CI commands reproduce (admin 54, trustee 32). Minors: VITE_CONSOLE_URL dual use (follow-up, TODOS); App tests mock the client module, so wire drift vs saksi main is only caught live (T3 covers it).
T2: complete pending merge (awaits T1 approval so both merge together with user go).
T1 fix round 1 (946c2bc + 3df0c6f): I1 operator view needs admin when auth on; I2 per-IP cap 50 checked first + per (ip, username) limit 5, both reserve before bcrypt, success resets own key, >256-byte names count against IP only; M1 publish scoped to run trustees; M2 dup/format trustee_id + bcrypt cost 12 enforced; M3 named ServeMux field; M4 comment; M6 runbook. 6 new tests, each failed against a broken copy. CI 8/8, -race clean (campaign 54 s due to cost-12 hashes). Ruling: accept 50-cap as 30 s lock-and-reset, mirroring the 5 limit. Scoped security re-review dispatched (opus).
T1 scoped security re-review (opus): READY TO MERGE. I1/I2/M1-M4/M6 closed and reproduced; 1,404-request harness 0 leaks; lockout concurrency holds under -race (60/100/20-way and 400x50 direct); per-account guessing 10/min unchanged; auth-off parity. New Minor (pre-existing): loopback source-address rotation (127.0.0.2/.3) gets fresh lockout budgets -> collapse loopback to one key; 2 comment nits. Fix round 2 sent. Advice kept: no lower-cost bcrypt test seam.
T1 fix round 2 (d7264a4): loopback collapsed to one lockout key; TestLoginLockoutTreatsEveryLoopbackAddressAsOne (controller verified: passes, fails without fix); fixture hashes hard-coded. MERGED #42 -> saksi adba922. T2 MERGED #56 -> balotachain b27938e. T3 smoke dispatched. T4 artifact refresh deferred until the study-wizard plan's W4 lands, so the mapping covers the new wizard.
T3 smoke (executor opus): 11/11 browser steps pass (run smoke-100-20260914-104829-1, E=0 on 6 contests, board verified BC-D259-9708); negative checks N1 N2 N3 N5 N6 pass (per-username lockout: trustee 429 + Retry-After 30 while admin 204), N4 not runnable offline (trail API 502 before seal check). Docs PR #57 (cbe4dfe). Defects: D1 Important — board download links point at admin-only /export/ (anonymous 401) and list ground-truth files; D2 trustee /events opened pre-sign-in never reconnects; D3 board "Open verifier" 502 offline (from #54); C1 admin tabs truncated at 1280 px; C2 check.go "V-100" message, favicon 401 noise.
Ruling: queue D1-D3, C1, C2 as one follow-up task after W1/W2 merge (D1's saksi part = a public verification-download route with a public-artifact allowlist + public favicon, which touches server.go/auth.go that W1/W2 are editing). Costs if wrong: the public board keeps broken download links with auth on until then; auth is off by default.
T4 (artifact refresh) deferred to after study-wizard W4.
