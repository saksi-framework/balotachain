# Ledger — 2026-09-14 study-grade wizard
Plan: docs/plans/2026-09-14-study-grade-wizard.md (approved 2026-09-14)
USER DECISIONS: D1 truly pause mid-submission for during-ballots attacks; D2 guarded browser buttons for network reset + peer restart; merge #42 and #56 when ready.
FINDING (controller, code read on saksi origin/main): wizard renders dkg/ballots attack panels only after /ceremony/start done (all ballots submitted, election closed); mountLiveAttack scores any submit error as PASS; closed-election gate (contract.go:173) rejects live ballot attacks first -> on-chain ballot/DKG verdicts can pass for the wrong reason. Also wizard concurrency default 8 (BatchTimeout trap).
W0: saksi #42 verified by controller (loopback-collapse test passes, fails without fix) and merged -> adba922; balotachain #56 squash-merged -> b27938e. Worktree balotachain-admin removal failed (Windows path length in node_modules); branch deleted; clean up later.
W1 dispatched (executor opus, saksi-attack-timeline, feat/attack-timeline off adba922). W2 dispatched (executor opus, saksi-wizard-campaigns, feat/wizard-campaigns off adba922). Plan committed on ci/audit-trail (joins docs PR #55).
FOLLOW-UP queued (after W1/W2 merge): smoke defects D1 (public verification download route + board links, no ground truth), D2 trustee SSE reconnect after sign-in, D3 verifier link offline, C1 tabs, C2 check.go message + public favicon.
W2: implemented saksi PR #43 (feat/wizard-campaigns: a1b08a4 feat, 6f83d17 wizard concurrency 128, 6acf1ac docs). In-process RoundTrip with unexported internalCallKey context -> auth-off path (full role bypass, needed for /ceremony/submit on offline runs); preflight block/warn rules as named constants; one job at a time; campaigns persisted <runs>/campaigns/<id>/{campaign.json atomic, summary.csv, log.txt}; streamed zip export. Real-binary tests with auth on: campaign 1+2 reps (3 rows, summary, export) and full ladder job (16 s). CI 8/8, -race green in WSL. Concerns: internal = auth off not admin; attack_plan drop on rebase with W1; cancelled campaign no summary; lazy interrupted status; disk.path uses the gate's volume. Review dispatched (final-reviewer opus): internal-caller forgeability + narrower role, CLI measurement parity, preflight source-of-truth, export allowlist.
W2 review (opus): internal path not forgeable from a real socket (headers/query/cookie/Host/absolute-form all 401/403); wizard campaign summary.csv identical to CLI (same writeSummaryCSV/collect, console timers; scratch side-by-side). NEEDS FIXES: I1 burst escapes preflight + Validate (reproduced: measured reps then ladder-gate failure, no summary); I2 internal marker = auth off -> narrow to driver's route/method allowlist, both auth modes; I3 /proc/loadavg in WSL2 blind to Windows host load (G7 not met) -> sample host CPU via interop. Minors M1,M3-M9,M11,M12 to fix; M10 doc; M13 defer to W3/W4. Fix round 1 sent.
W1: implemented saksi PR #44 (feat/attack-timeline, 3400b7a..6afe523). Gate table: tamper-dkg-transcript -> auditor dkg.decode, NO on-chain gate (shape only), simulated at dkg pause "accepted by design"; tamper-ballot-proof -> cds, live at ballots pause; reused-nullifier -> nullifier, live (committed ballot's nullifier); corrupted-ballot-bytes -> decode, live; dropped-ballot -> auditor stream.completeness, simulated at close; reordered-ballots -> no gate anywhere, SKIPPED; tamper-partial-decryption -> auditor decryption.cp_proof, NO on-chain proof check, simulated at ceremony pause "accepted by design". THREE mutations were targeting the wrong field (e.g. partial decryption rewrote contest id) -> earlier verdicts invalid. Chaincode: gate=<id>: error prefix (redeploy on WSL); saksi-demo audit-stream --json lists failed checks (rebuild). Ballots pause: stop at ballots_at x N, drain, mount, resume; window_ms sums halves. security_run perf column. CI 8/8, -race WSL ok, Rust ok, headless-browser pause flow (2 bugs fixed). Concerns: gRPC error details may not carry gate text on real Fabric (would read INCONCLUSIVE, W6 confirms); W2 must drop attack_plan; old saksi-demo fails TestScenariosRejectTheirMutations.
W1: review dispatched (final-reviewer opus): gate table truth vs contract.go check order, mutation fixes + invalid earlier verdicts, gate prefix transport through fabric-gateway errors, pause semantics/no-plan parity, mount-context columns.
NOTE for thesis docs after merge: CLAIMS.md RQ2 rows need updating (no on-chain DKG integrity gate; partial-decryption proof not verified on-chain — already stated; reordered-ballots has no gate; earlier verdicts for 3 mis-targeted mutations invalid).
W2 fix round 1 (0a7f2da + 212cb01): I1 burst validated + gated before any rep; I2 internal allowlist (unchanged from review list, re-derived from repeat.go/RunLadder) enforced both auth modes; I3 Windows host CPU via powershell interop on WSL (live: guest load1 0.79 vs host 44% — blind spot confirmed, now detected); M1,M3-M9,M11,M12 code, M10 runbook, M13 deferred. 8 new tests. CI 8/8, -race green incl. real-demo campaign + ladder. Scoped re-review dispatched (opus).
W1 review (opus): NO CRITICAL; both bars hold. Verified live mutations vs real chaincode + real crypto (reused-nullifier->gate=nullifier, corrupted-bytes->decode, proof on unsent->cds; after close->election-open) and gate id transport through real fabric-gateway v1.7.1 EndorseError via clientsdk.ErrorText (err.Error() alone never carried it). NEEDS FIXES: I1 reordered-ballots claims nonexistent chaincode digest gate; I2 step-7 Run again overwrites staged live verdict in negative-tests.csv; I3 "accepted by design" understates NO CALLER AUTHORIZATION (GetClientIdentity never called) -> front-running DoS: tampered DKG transcript blocks real one (dkg-duplicate) and ballots fail ungated; front-run trustee partial blocks real share (partial-duplicate). Minors 4-12 fix; 13 (campaign can start during paused security run) -> W3 busy guard.
EVIDENCE RULING: discard ALL earlier on-chain attack rows (saksi <= adba922: any error scored PASS, chaincode reason never received). Offline: re-run corrupted-ballot-bytes and tamper-partial-decryption (mutations changed); tamper-dkg-transcript offline valid (12/12 dkg.decode); proof/nullifier/dropped valid. Plan §2 rule 4 to amend.
W1: fix round 1 sent (I1-I3, M4-M12). Chaincode caller authorization NOT added (separate decision for user).
W2 scoped re-review (opus): READY TO MERGE (I1-I3, M1, M3-M11 closed; burst now 409 before any rep; allowlist before auth-off shortcut, derived coverage test; real WSL2 sampleHost ok; summary.csv parity PASS; forgery probes 401/403). New minors N1 preflight powershell spawn uncached (auth-off loopable), N2 noisy 1-shot CPU reading, N3 disk projected per single run not whole campaign (capstone risk), N4 test comment, N5 nil Body in internal transport. Fix round 2 sent (all five) before merge.
RATE LIMIT: W1 fix round 1 and W2 fix round 2 agents terminated (session limit) before any edit; both worktrees clean at 6afe523 / 212cb01. User enabled /low-priority. Both resumed with instructions to commit in small steps.
W2 fix round 2 (98aca86 N3; bfcc0a9 N2/N1/N5/N4): whole-campaign disk projection (force cannot override), Get-Counter 3 samples avg last 2 (live WSL 14.5/13.5/18.1), 10 s singleflight preflight host cache, NoBody + loopback RemoteAddr, TestInternalRoutesCoverTheDriverOnErrors added. CI 8/8, -race green. Scoped check dispatched (sonnet).
W1 fix round 1 (4c82209 I1, ebc24f0 I2, 7bd7361 I3, 39e0e7d M4-9,11, 6fc771d M10,12): all closed per implementer; M5 chose /submit pauses at ceremony before PublishTally; 9 campaign tests + chaincode TestLiveAttackMutationsMeetTheirDeclaredGateFirst; CI 8/8, -race green. Open gap: offline /run-all with attack_plan marked security_run without pausing (errs toward RQ3 exclusion). Scoped re-review dispatched (opus).
W2 fix round 2 scoped check (sonnet): APPROVED. N3 disk formula uses the same LedgerBytesPerBallot + source as diskGate, maxSweepSteps=12 is the real loop bound, force cannot override; N2 real Get-Counter output parsed, missing counter -> null (verified live); N1 singleflight cache race-free by inspection; N5 loopback RemoteAddr changes no authorization (trail route not in internal allowlist, no session). Nits (test coverage only): empty-stdout Get-Counter case untested; on-chain iterations for 4 ceremony routes in the error-branch test are no-ops. W2 READY TO MERGE (PR #43, head bfcc0a9) — merge together with #44 when its re-review clears.
W1 scoped re-review (opus): code for I2, I3, M4-M12 CLOSED (merge rule held-back + journal; notes + findings verified vs contract.go; chaincode test pins nullifier-before-cds; decision race closed). NEEDS FIXES (docs): I1 still claimed in docs/wizard/deep-dive.md:334-337,:349 ("ordering enforced at endorsement") — defense-panel doc. Recommended: 400 on offline /run-all with attack_plan (plan silently ignored). New minors: ceremony-pause test blind (no partials), vacuous "all upheld" when mounted=0. Fix round 2 sent (all four).
OUT-OF-PR FINDING (thesis material): saksi-auditor adversary table + tests claim "reorder detected by ledger digest" (security_privacy.rs:18,:72-98; independent_verification.rs:100-115) but ledger::ledger_digest is only called from tests; no verifier uses it -> user decision: wire a digest check into the verifier or correct the claims. Recorded in TODOS.
USER: merge #44 then rebase+merge #43; reordering claim -> correct it (not detected; tally order-independent). MERGED #44 -> saksi eabe840.
W2: rebase onto eabe840 + c.AttackPlan=nil before Validate + test + run_busy preflight check (if cheap) sent to W2 executor. Reordering claim fix dispatched (executor opus, saksi-reorder-claim, fix/reordering-claim): correct adversary table + tests to 'not detected; tally order-independent'.
MERGED #43 -> saksi 0cb72ce (rebased on #44; c.AttackPlan=nil before Validate with TestCampaignDropsAttackPlan; run_busy preflight block, not forceable). Worktree removed.
PR #45 (fix/reordering-claim, e37ea52): adversary table + 2 tests corrected to "not detected; tally order-independent"; digest-only asserts moved to ledger unit test; controller verified only test code changed, no remaining claim, CI 8/8. Merge awaits user go.
VALIDATION (user-resumed 10K agent): completed runs incl. clean MP-10K rerun mp-10k-val3 (TPS 909-1012, p99 215-238) but note uncommitted and §3b misattributes MP-10K slowdown to thermal throttling (agent's own last message: game sp22-cod ~43% of cores = contention). Note's verdicts so far: PR #40 confirmed all 4 tiers; PR #41 confirmed at 10K (SP-10K 1/10 reps 5.8 s -> 0/5; MP-10K 2/10 20.0 s -> 0/5). Resumed agent to fix cause, add val3 analysis, commit (analysis only, no runs). WSL: Docker Desktop stopped; nothing running.
W3 dispatched (executor opus, saksi-infra, feat/wizard-infra off 0cb72ce): network reset job (admin+loopback+confirm RESET, busy guards, tier.sh, identity reload), peer-restart fault (admin+loopback+confirm RESTART, on-chain security runs, journal events, drops->resume->verify), live WSL check doubling as chaincode redeploy (gate text through Fabric transport).
USER: merge #45, #55, #57. MERGED saksi #45 -> 5420034 (reordering claim corrected). MERGED balotachain #57 (smoke write-up) and #55 (thesis docs + plans, squash) -> b91cea9. Note: ci/audit-trail continues to receive docs commits (validation note fix pending); later PRs from it will diff only new changes.
VALIDATION FINAL (03509df, pushed on ci/audit-trail): §3b corrected to host CPU contention (game); val2 contended (3.89-4.23 ms/record); val3 clean (2.28-2.39 ms/record, 6 of 7 reps); MP-10K val3 pauses 2/10 20.0 s -> 0/5 0.0 s, min TPS 571.7 -> 909.2, p99 226.4 -> 217.0; PR #41 CONFIRMED at both 10K tiers on a clean host, not testable at 1K; PR #40 confirmed all four tiers. Needs a follow-up docs PR to main (branch was squash-merged in #55).
W3: implemented saksi PR #46 (feat/wizard-infra: 85f2b64 code, 851ef86 runbook). Reset: admin+loopback, confirm RESET, 400 no Fabric, 501 no tier.sh, 409 busy, 15-min job with process-group kill, network-resets.log. Fault: admin+loopback, confirm RESTART, at (0,1), down_s 5-120, on-chain, not campaign, no attack plan, armed only before ballots stage; stops only peer0.org1.example.com while dispatch continues; fault.start/end/peer_ready journal; lossy window marked interrupted for Resume. Identity: Connect re-reads per call; cached /api/trail connection dropped on reset. CI 8/8, -race WSL ok.
LIVE WSL (chaincode redeployed from main): (a) reset 94 s exit 0; SP-1K with no console restart, 50/block; tamper-ballot-proof PASS gate cds, actual "rejected by chaincode gate cds (the declared gate): contest "president/cand0" CDS well-formedness proof failed: CDS branch 0 verification equation failed"; nullifier + decode attacks PASS on their gates -> W1 open question RESOLVED (gate text survives Fabric transport). (b) SP-10K fault at 0.5 down 20 s: peer stopped at 5000, 5000 dropped, peer ready 1.06 s after restart; Resume found 54 "dropped" already on chain, committed 4946 with 0 drops; Verify E=0, ledger_matches_local true -> T3 resilience result.
Concerns: Resume now closes the election on every resume (behaviour change); faulted window stopped:true though all dispatched; ladder refuses while run busy; peer_ready ts includes ~4 s old-connection wait.
W3: review dispatched (final-reviewer opus): guard bypass from real socket, argv/command injection, process-group kill, fault goroutine lifetime (peer left down?), resume-closes-election correctness, live-result consistency.
W4 dispatched (executor opus, saksi-wizard-ui, feat/wizard-study-ui off 5420034) in parallel with W3 review: sign-in, mode switch, preflight panel, run-table presets, campaign view w/ polling + export, ladder job, attack-timeline editor, runs list resume/verify-only, infra panel (against PR #46 contract; reconcile after review), help links; Playwright tests on Windows scratch port offline. Ruling: start W4 before W3 merges to save wall time; costs if wrong: infra-panel rework if the W3 review changes the fault/reset contract.
W3 review (opus): NO guard bypass, NO command injection (real-socket probes: LAN IP as admin with XFF/X-Real-IP/Forwarded -> 403; confirm variants 400; path tricks 404/400; string/float voters 400); argv-only exec; process-group kill verified on Linux; Connect re-reads identity per call; live T3 numbers consistent with code (block heights, 54 replays counted). NEEDS FIXES: I1 campaign busy race (reproduced; startExclusiveJob), I2 fault ignores other busy runs/jobs (reproduced), I3 peer left down if console dies mid-fault (runbook + recover). Resume recommendation: keep close-on-resume, explicit error before close when stopped, retryable close (already-closed = success; close-only resume). Minors M1 retryable close (reproduced stuck), M2 remaining=0 after fault (W4 told), M3 closed-loop drops within ~1 s regardless of down_s + verify-only required, M4-M10. Fix round 1 sent to W3; notes 1-5 sent to W4.
Plan amendment needed: §4 fault row "Security runs only" -> "on-chain, no attack plan, window_s 0; arming makes the run a security run".
THESIS NOTE (T3): with closed-loop dispatch, all ballots after the fault point drop within ~1 s regardless of outage length; the resilience claim is "no committed ballot lost; resume recovers all pending via the nullifier set (54 committed-but-reported-dropped replays skipped); audit E=0, ledger matches". Outage-window loss needs send_rate.
W3 fix round 1 (rebased on 5420034; 149d483 code+tests, ff7d4cc runbook): I1 startExclusiveJob (TestCampaignRefusesARunThatStartedDuringPreflight), I2 arming + firing refusal (2 tests), I3 SIGINT/SIGTERM handler + panic recover restart peer, fault.panic; resume stopped part-way errors before close; retryable close-only resume + already-closed = success; M2 estimate upper bound; M3-M10 done. CI 8/8, WSL -race 126 s. Not re-run live (would wipe W3's elections; WSL console on old 851ef86 build -> rebuild before W4/W6). Scoped re-review dispatched (opus).
W4: implemented saksi PR #47 (feat/wizard-study-ui, b80ea03..51c1410). Screens: sign-in/out, mode switch, preflight (gates Start, force for fabric_unreachable), run-table presets, campaign view (3 s poll) + list, ladder job, attack timeline editor, runs list Resume/Verify-only, infra panel (vs #46 851ef86 + notes; merges with ff7d4cc), help links. Playwright Windows: auth off 45/45, auth on 26/26. Server changes: /wizard public (sign-in reachable); /runs adds status/resumable/was_interrupted/resume_pending/reconciled (public route!). CI 8/8.
FINDINGS from W4: (1) OfflineVoterCeiling 10,000 still in config.go -> offline capstone row 8 (MP-483K..3.5M) not runnable from wizard (plan Perf 1 said remove once streaming generator landed); (2) on-chain campaign rep with Fabric connect failure recorded failed:false, empty TPS, runs_failed 0 (pre-existing, CLI too) -> silently wrong artifact, must fix before W6; (3) resume without Fabric/bundle returns 202 then silent.
W4: review dispatched (sonnet) incl. /wizard public + /runs field exposure judgement and contract drift vs #46 ff7d4cc.
QUEUED "W4b" (saksi, after #46 + #47 merge): submit-stage errors reach run.end/perf.csv (finding 2); lift OfflineVoterCeiling with an offline disk/memory preflight (finding 1); synchronous resume refusal without Fabric/bundle (finding 3); smoke defects D1 public verification-download route + board links (no ground truth), C2 check.go message + public favicon. Balotachain side (D2 trustee SSE reconnect, D3 verifier offline, C1 tabs) as a separate small PR.
W3 scoped re-review (opus): READY TO MERGE. I1/I2/I3 + resume + M2-M10 closed with reproductions (campaign race 409; fault beside busy run docker=[]; stuck close recoverable; cancelled resume doesn't close); no deadlock (lock order traced); real-socket probes unchanged; range-diff confirms rebase clean. New: N1 another run can start while peer down (claim only refuses during reset); N2 lossy resume stuck (pre-existing, hits T3); N3 Ctrl-C during docker stop; N4 signal.Stop; N5 tighten already-closed match; N6 typo + stale doc. Fix round 2 sent (all). Live checks before W6: (1) real reset ends done with chain_height; (2) T3 on new build, window_conn_ready true <3 min, refusals; (3) Ctrl-C during down_s restores peer; (4) second CloseElection text through real gateway contains "is already closed". Plus -race for fault/resume/campaign tests in WSL.
W4 review (sonnet): NEEDS FIXES. I1 followReset clobbers post-reset connect-check error (chain_error/job error) -> fix; I2 public /runs exposes raw run.end reason (Go error text, possible internal addresses) -> Ruling: reason only for admin session or auth off; anonymous gets coarse status fields. Minor: pin fault-eligibility filter to server rule. Presets verified vs cost-model run table. Fix round 1 sent to W4. Resume-without-Fabric synchronous refusal added to W3 fix round 2 (owning branch). Costs if ruling wrong: wizard shows reason only when signed in as admin.
W3 fix round 2 (fa4f4f8): N1-N6 + resume synchronous refusals; controller ran TestNoPhaseStartsWhileAFaultHoldsThePeer, TestALossyResumeStaysResumable, TestResumeFailsOnAGenuineDrop, TestRestorePeerWaitsForAStopInFlight, TestResumeAPIRefusesBeforeDispatch -> PASS; CI 8/8. USER: merge #46. MERGED #46 -> saksi eb64c90. W4 fix round 1 (c8a6210, 2f8e2c5): reset error/chain_height shown; reason gated (TestRunsReasonOnlyForAdminsOrAuthOff); TestWizardFaultFilterMirrorsTheFaultRoute; Playwright 9/9 + 45/45 + 27/27 against branch merged with fa4f4f8; CI 8/8. Rebase pending #46 merge -> now.
W4 rebase onto eb64c90 requested. Balotachain smoke UI fixes dispatched (executor sonnet, balotachain-uifix, fix/smoke-ui): D2 trustee SSE reconnect after sign-in, D3 board verifier unavailable offline, C1 admin tabs at 1280/1440. D1 + C2 wait for the saksi follow-up (W4b) after #47 merges.
PR #58 (fix/smoke-ui): D2/D3/C1 correct (review verified vs saksi board.go); lint failed on unused mock params -> controller fixed with typed vi.fn (lint/typecheck/33 tests/format pass), pushed. Awaiting CI + user merge go.
W4 rebased onto eb64c90 clean (head 0ca579b); /runs close-pending sync with #46 retryable close (own commit); Playwright 9/9, 45/45, 28/28; CI 8/8 (TestSingleRunLockReturns409 flake once). Scoped re-review dispatched (sonnet): I1 reset error display, I2 reason gating, M3 fault filter pin, close-pending derivation vs planResume.
USER: merge #47 + #58. MERGED saksi #47 -> 0d9bdaf; balotachain #58 -> a79918d.
Worktrees balotachain-uifix + balotachain-admin: git-pruned, folders remain on disk (Windows long paths); harmless. W4b dispatched (executor opus, saksi-w4b, fix/study-followups off 0d9bdaf): submit-stage failures fail the run + nothing_submitted predicate; lift OfflineVoterCeiling with offline disk + memory preflight; public board verification download allowlist (D1) + list in /api/board; C2 check.go padding + public favicon. W5 dispatched (executor opus, saksi-w5, docs/study-from-wizard): runbook §10 Running the study from the wizard (Troubleshooting -> §11), docs/study-checklist.md, help links.
W5: implemented saksi PR #48 (docs/study-from-wizard, de09ee0): runbook §10.1-10.9 (Troubleshooting -> §11), docs/study-checklist.md (91 lines), row 8 marked <!-- W4b -->, anchor verified on GitHub. MISSING CAPABILITIES found: (1) no wizard fields for sweep/burst (row 5); (2) 60-min phase timeout not configurable -> SP-3.5M only via Resume (no RQ3), row 9 verify ~2.4 h can't finish [capstone blocker]; (3) wizard can't reopen a run to continue trustees/publish/verify (resumed campaign rep or reloaded page stuck); (4) export lacks journal, gen-timings.json, receipts -> cost_model.py can't read it (W6 check fails as written); (5) runbook §9 vs §10 T3 order contradiction.
Ruling: server-side (2), (4), (5) added to W4b as items 6-8; UI (1), (3) queued as W4c after W4b merges; PR #48 held open until W4b + W4c land so its limit paragraphs and <!-- W4b --> blocks are rewritten against fixed code. balotachain follow-up queued: board links -> public files route; cost_model.py reads the campaign export layout.
W4b: subagent stopped at user request ("move that subagents work to this main conversation"); controller finished it in-session. Items 1-2 were committed by the agent (6ccb6e5, 30e7d01; reviewed). Controller: item 3 no change (409 before dispatch, tested); 4 public files route 8aa6873 (header projection strips ground_truth + voter_ids, 409 while sealed, correctness.csv admin-only); 5 04924b7; 6 --phase-timeout 0d1d696; 7 export journal/gen-timings/receipts-lifecycle 8320f91; 8 runbook §9 T3 order 06da80b. Windows full suite green with real saksi-demo (20K offline run passes); WSL -race green. saksi PR #49 opened. Report w4b-report.md. Awaiting user go to merge; then W4c.
W4b MERGED: saksi PR #49 -> 25d523f (user: merge when CI green; 8/8 checks pass). Next W4c in worktree saksi-w4c off main.
W4c PAUSED (user switched to artifact refresh + Defense Reviewer codebase section, 2026-09-15). Worktree saksi-w4c (feat/wizard-w4c off 25d523f), no edits. Design so far: (a) campaignBox fields sweep (x/step, 0 off), windowS (default 120), burst (voters, 0 off) sent by startCampaign only when set; live hint of step rates (start = send rate, 10/s when 0; 12 steps max; 10/s x1.5 tops out ~865/s < ~1000 TPS network, so set send rate); presets untouched. (b) runs list "Open" for non-busy, non-resumable runs: ended/failed with correctness.csv -> go(5)+loadResults; ceremony closed_at or published -> startCeremony(); else runCheck(); must set runID/cfg, showIDChip, stop timers/streams, setMode single; add openRun to web_test function list. (c) PF_ROWS "Phase timeout" row, codes phase_timeout_short/tight, fact from rep.phase_timeout.
2026-09-15 ARTIFACT + REVIEWER (plan hazy-beaming-willow, approved): Saksi Execution Trace republished (Version 7, same URL) pinned saksi 25d523f / balotachain a79918d: 101 function sources extracted by scratchpad/trace/extract_fns.py (0 line mismatches), fresh offline run halalan-e2e-20260914-174438-1 (1000x3x12, E=0, publish 409/409/202, 6 PASS + 1 SKIPPED), new sections (study harness, apps->API->Go, route/role table, repo map), corrected guarantees/limitations. Defense Reviewer: PART 5 codebase (5 pages, 22-26) appended via docs/defense-reviewer/{part5-codebase.html,build.py} (Edge print + PyMuPDF header stamp); pages 1-21 text identical; both repo copies replaced (original kept in Downloads). FINDING: generator DKG dealers fixed (fixtures.rs gen_prologue, coeff dealer_id*13+k+1) -> harness election keys derivable; contradicts manuscript "OS CSPRNG" claim; not in CLAIMS.md/TODOS yet (needs user go). W4c still paused.
